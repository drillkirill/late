import React from 'react'
import './Button.css'

const onRec = () => {
    ///
}

const Button = () => {
    <div>
        <button onClick={onRec}>Записаться</button>
    </div>
}

export default Button;